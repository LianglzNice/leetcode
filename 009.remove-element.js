/**
 * @param {number[]} nums
 * @param {number} val
 * @return {number}
 */
let nums = [0,1,2,2,3,0,4,2], val = 2
let removeElement = (nums, val) => {
    for(let i=0; i<nums.length-1; i++){
        for(let j=i; ){

        }
    }
};